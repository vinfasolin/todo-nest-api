{
  "name": "todo-nest-api",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "build": "prisma generate && nest build",

    "start": "node -e \"const fs=require('fs');const path=require('path');const cp=require('child_process');const exists=(p)=>fs.existsSync(p)&&fs.statSync(p).isFile();const run=(cmd)=>cp.execSync(cmd,{stdio:'inherit'});if(!fs.existsSync('dist')){console.log('dist/ not found -> running build...');run('npm run build');}const candidates=['dist/main.js','dist/main.mjs','dist/main.cjs','dist/src/main.js','dist/src/main.mjs','dist/src/main.cjs'];let entry=candidates.find(exists);if(!entry){const found=[];const walk=(dir)=>{for(const n of fs.readdirSync(dir)){const p=path.join(dir,n);const st=fs.statSync(p);if(st.isDirectory()) walk(p); else {const b=path.basename(p); if(b==='main.js'||b==='main.mjs'||b==='main.cjs') found.push(p);}}};walk('dist');entry=found[0];}if(!entry){console.error('❌ Could not find main entry inside dist/. Run: npm run build and inspect dist/.');process.exit(1);}console.log('▶ starting (require):', entry);process.on('uncaughtException',(e)=>{console.error('🔥 uncaughtException');console.error(e);process.exit(1);});process.on('unhandledRejection',(e)=>{console.error('🔥 unhandledRejection');console.error(e);process.exit(1);});try{require(path.resolve(entry));}catch(e){console.error('🔥 start failed');console.error(e);process.exit(1);}\"",

    "start:dev": "nest start --watch",
    "start:debug": "nest start --debug --watch",

    "format": "prettier --write \"src/**/*.ts\" \"test/**/*.ts\"",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage",
    "test:debug": "node --inspect-brk -r tsconfig-paths/register -r ts-node/register node_modules/.bin/jest --runInBand",
    "test:e2e": "jest --config ./test/jest-e2e.json"
  },
  "dependencies": {
    "@nestjs/common": "^11.0.1",
    "@nestjs/core": "^11.0.1",
    "@nestjs/jwt": "^11.0.2",
    "@nestjs/platform-express": "^11.0.1",
    "@prisma/adapter-pg": "^7.4.0",
    "@prisma/client": "^7.4.0",
    "google-auth-library": "^10.5.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.18.0",
    "prisma": "^7.4.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3.2.0",
    "@eslint/js": "^9.18.0",
    "@nestjs/cli": "^11.0.0",
    "@nestjs/schematics": "^11.0.0",
    "@nestjs/testing": "^11.0.1",
    "@types/express": "^5.0.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^22.10.7",
    "@types/supertest": "^6.0.2",
    "eslint": "^9.18.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-plugin-prettier": "^5.2.2",
    "globals": "^16.0.0",
    "jest": "^30.0.0",
    "prettier": "^3.4.2",
    "source-map-support": "^0.5.21",
    "supertest": "^7.0.0",
    "ts-jest": "^29.2.5",
    "ts-loader": "^9.5.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.7.3",
    "typescript-eslint": "^8.20.0"
  },
  "jest": {
    "moduleFileExtensions": ["js", "json", "ts"],
    "rootDir": "src",
    "testRegex": ".*\\.spec\\.ts$",
    "transform": {
      "^.+\\.(t|j)s$": "ts-jest"
    },
    "collectCoverageFrom": ["**/*.(t|j)s"],
    "coverageDirectory": "../coverage",
    "testEnvironment": "node"
  }
}
